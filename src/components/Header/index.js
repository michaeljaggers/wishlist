import React from 'react';
import Navigation from '../Navigation';

const Header = () => (
  <div>
    <h4 className = "bg-primary text-white text-left p-2" >Wish List</h4>
    <Navigation />
      <hr />
  </div>
);

export default Header;